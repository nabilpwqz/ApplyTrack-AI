# ApplyTrack AI - Server Service (Express + TypeScript)

This is the Express TypeScript backend service for ApplyTrack AI.

## Quick Start
```bash
npm install
npm run dev
```
* **Dev Server:** `http://localhost:5000/api`
* **Production Build:** `npm run build` (`tsc`)
